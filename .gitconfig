[user]
    name = cynful
    email = cynthia.kwok@joinhoney.com
[alias]
    # one-line log
    l = log --pretty=format:"%C(yellow)%h\\ %ad%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --date=short

    a = add
    ca = commit -a
    cm = commit -m
    cam = commit -am
    d = diff
    s = status -s
    co = checkout
    cob = checkout -b

    la = "!git config -l | grep alias | cut -c 7-"
